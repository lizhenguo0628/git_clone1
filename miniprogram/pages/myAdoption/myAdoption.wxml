

<view wx:for="{{animalInfo}}" wx:key="index" id="{{item._id}}" bindtap="toAdoptionDetail">
    <image src="{{item.fileID}}"></image>
    <view>名字：{{item.name}}</view>
    <view>年龄：{{item.age}}</view>
    <view>{{item.text}}</view> 
  </view>