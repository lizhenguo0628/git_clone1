<view>

</view>
<view>
  <view>助力爱心</view>
  <view>寻求帮助</view>
  <view>流浪动物救助，正在直播</view>
  <view wx:for="{{animal}}" wx:key="index" id="{{item._id}}" bindtap="toDetail">
    <image src="{{item.fileID}}"></image>
    <view>名字：{{item.name}}</view>
    <view>年龄：{{item.age}}</view>
    <view>{{item.text}}</view> 
  </view>
</view>